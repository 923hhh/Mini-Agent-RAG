# Mini-Agent-RAG2 优化方案

---

## 一、必须做（阻塞性问题）

### M1 — API Key 泄露（立即处理）

`configs/model_settings.yaml` 中有两个真实密钥已提交到 git：
- DeepSeek key：`sk-***`
- 火山引擎 key：`***`

**方案：** 立即在对应平台轮换这两个 key，然后把 `model_settings.yaml` 改为从环境变量读取，提供 `model_settings.yaml.example` 模板。

### M2 — Embedding 只支持 Ollama，不支持 openai_compatible

`embedding_service.py` 硬编码返回 `OllamaEmbeddings`，但当前配置 `LLM_PROVIDER=openai_compatible`。用户如果没有本地 Ollama，知识库构建和检索全部失败。

**方案：** 仿照 `llm_service.py` 的 provider 分支，为 `embedding_service.py` 增加 `openai_compatible` 路径（支持 OpenAI 兼容的 embedding 接口，如 DeepSeek、SiliconFlow 等）。

### M3 — `load_settings()` 每次请求都重读 3 个 YAML

每个 API 请求都触发 3 次磁盘 I/O，高并发下是明显瓶颈。

**方案：** 用 FastAPI `lifespan` 在启动时加载一次，通过依赖注入传递，或加 `@lru_cache`。

---

## 二、可选优化（质量提升）

### O1 — `documents.py` 拆分（2790 行单文件）

当前一个文件承担：格式解析、OCR 流水线、VLM 调用、图像预处理、指令页检测、元数据提取。

**方案：**
```
app/loaders/
  ├── text.py       # txt/md
  ├── pdf.py        # PDF + 扫描件
  ├── office.py     # docx/epub
  ├── image.py      # 图像 + OCR
  ├── vlm.py        # VLM 描述调用
  └── factory.py    # KnowledgeFactory 分发
```

### O2 — 提取重复代码

| 重复项 | 出现位置 | 提取目标 |
|--------|----------|----------|
| `_coerce_optional_text()` | `kb_incremental_rebuild.py` + `embedding_assembler.py` | `app/utils/text.py` |
| `IMAGE_QUERY_HINTS` | `rag.py` + `local_kb.py` | `app/constants.py` |
| header 提取逻辑 | `embedding_assembler.py` + `local_kb.py` | `app/utils/text.py` |
| 去重函数 | 3 处不同实现 | 统一为一个 |

状态：已完成（2026-04-11，文本/头部/图片 query 常量与剩余去重辅助已统一到公共模块）

### O3 — 知识库重建接口异步化

`POST /knowledge_base/rebuild` 是同步阻塞的，大型知识库会超时（已知问题，TODO 文档有记录）。

**方案：** 改为异步任务模式：
- `POST /knowledge_base/rebuild` 返回 `{task_id, status}`
- `GET /knowledge_base/rebuild/{task_id}` 查询进度和结果

### O4 — 删除 `agents/executor.py`

该文件只有两行 re-export，没有实际价值，增加导入路径混乱。直接删除，调用方改为从 `multistep.py` 导入。

### O5 — Tesseract 路径配置化

`kb_settings.yaml` 中硬编码了开发者本机路径 `C:/Users/ASUS/tesseract-local/tesseract.exe`，其他人克隆后直接失败。

**方案：** 改为相对路径 `./data/tools/Tesseract-OCR/tesseract.exe`，或支持环境变量覆盖。

### O6 — Agent 工具选择升级（Phase L 前置）

当前基于关键词列表匹配，语义模糊时不稳定。

**方案：** 接入 LLM 原生 tool calling（DeepSeek 已支持），替换关键词匹配逻辑。

---

## 三、目录结构（优化后）

```
Mini-Agent-RAG2/
├── app/
│   ├── api/
│   │   ├── main.py
│   │   ├── chat.py
│   │   ├── knowledge_base.py
│   │   ├── tools.py
│   │   ├── errors.py
│   │   └── streaming.py
│   ├── agents/
│   │   └── multistep.py          # 删除 executor.py
│   ├── chains/
│   │   ├── rag.py
│   │   └── text_splitter.py
│   ├── constants.py              # 新增：IMAGE_QUERY_HINTS 等共享常量
│   ├── loaders/                  # 拆分 documents.py（O1）
│   │   ├── factory.py
│   │   ├── text.py
│   │   ├── pdf.py
│   │   ├── office.py
│   │   ├── image.py
│   │   └── vlm.py
│   ├── retrievers/
│   │   └── local_kb.py
│   ├── schemas/
│   │   ├── chat.py
│   │   ├── kb.py
│   │   └── common.py
│   ├── services/
│   │   ├── settings.py           # 改为启动时加载（M3）
│   │   ├── embedding_service.py  # 增加 openai_compatible 分支（M2）
│   │   ├── llm_service.py
│   │   ├── kb_ingestion_service.py
│   │   ├── kb_incremental_rebuild.py
│   │   ├── embedding_assembler.py
│   │   ├── query_rewrite_service.py
│   │   ├── rerank_service.py
│   │   ├── temp_kb_service.py
│   │   ├── observability.py
│   │   ├── reference_overview.py
│   │   ├── network.py
│   │   └── init_service.py
│   ├── storage/
│   │   ├── vector_stores.py
│   │   └── filters.py
│   ├── tools/
│   │   └── registry.py
│   ├── ui/
│   │   └── app.py
│   └── utils/                    # 新增：提取重复工具函数（O2）
│       └── text.py
├── configs/
│   ├── basic_settings.yaml
│   ├── kb_settings.yaml
│   ├── model_settings.yaml       # 改为从环境变量读取密钥（M1）
│   └── model_settings.yaml.example  # 新增模板
├── scripts/
│   ├── init.py
│   ├── rebuild_kb.py
│   ├── start_api.py
│   ├── start_ui.py
│   ├── cleanup_temp_kb.py
│   ├── eval_retrieval.py
│   └── validate_phase7.py
└── requirements.txt
```

---

## 四、接口设计（现有 + 建议补充）

### 现有接口（保持不变）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/chat/rag` | RAG 问答，支持 SSE 流式 |
| POST | `/chat/agent` | Agent 多步推理，支持 SSE 流式 |
| POST | `/knowledge_base/upload` | 上传文件（scope=local/temp） |
| POST | `/knowledge_base/rebuild` | 重建知识库索引 |
| GET | `/knowledge_base/list` | 列举知识库 |
| GET | `/tools` | 获取工具定义 |

### 建议新增（对应 O3）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/knowledge_base/rebuild` | 改为返回 `{task_id, status}` |
| GET | `/knowledge_base/rebuild/{task_id}` | 查询重建进度 `{status, progress, result}` |

---

## 五、优先级排序

| 优先级 | 项目 | 原因 |
|--------|------|------|
| P0 | M1 API Key 轮换 | 密钥已公开，安全风险 |
| P1 | M2 Embedding 多 provider | 当前配置下功能不完整 |
| P1 | M3 settings 缓存 | 每请求读 YAML，性能问题 |
| P2 | O5 Tesseract 路径 | 影响其他人复现 |
| P2 | O4 删除 executor.py | 低成本清理 |
| P3 | O2 提取重复代码 | 可维护性 |
| P3 | O3 重建接口异步化 | 已有 CLI 绕过方案，不紧急 |
| P4 | O1 documents.py 拆分 | 工作量大，可分批做 |
| P4 | O6 Agent tool calling | 功能增强，Phase L 范围 |

---

## 六、近期文献技术手段映射

> 基于 2024–2025 年 RAG 主流文献，结合当前项目代码现状，筛选出可落地的技术手段。

---

### T1 — Multi-Query 多角度查询扩展

状态：已完成（2026-04-11，原始问题 + 多角度改写，检索层已接入）

**参考文献：** [Advanced RAG: Query Rewriting, Multi-Step Retrieval, and Fusion](https://tutorialq.com/ai/dl-applications/advanced-rag)（2025）

| | 说明 |
|---|---|
| **现状** | `query_rewrite_service.py` 每次只生成 1 个改写查询，单路检索，口语化问题召回率低 |
| **方案** | 同时生成 3 个角度的查询（原始 + 改写 + 反向假设），分别检索后 RRF 融合，扩大召回覆盖面 |
| **改动范围** | `query_rewrite_service.py` 增加 `generate_multi_queries()` 方法；`local_kb.py` 融合多路结果 |
| **优先级** | P1 — 改动小，收益直接 |

---

### T2 — HyDE 假设文档嵌入

状态：已完成（2026-04-11，默认关闭，仅追加到 dense 检索路径）

**参考文献：** [Advanced RAG Output Grading](https://ar5iv.labs.arxiv.org/html/2404.01037)（2024）

| | 说明 |
|---|---|
| **现状** | 直接用用户问题向量检索，问题语义空间与文档语义空间存在偏差，专业领域问题尤为明显 |
| **方案** | 用 LLM 先生成一段"假设答案文本"，用假设答案的向量做稠密检索，原始问题继续做 BM25 检索，两路 RRF 融合。实验证明对专业领域问题 Top1 命中率提升显著 |
| **改动范围** | `query_rewrite_service.py` 增加 `generate_hypothetical_doc()` 方法；`local_kb.py` 增加 HyDE 检索路径，受 `ENABLE_HYDE` 配置开关控制 |
| **优先级** | P1 — 与 T1 可同批落地 |

---

### T3 — LLM 原生 Tool Calling 替换关键词匹配

**参考文献：** [Agentic Retrieval-Augmented Generation Survey](https://arxiv.org/html/2501.09136v2)（2025.01）；[Agentic RAG vs Classic RAG: From a Pipeline to a Control Loop](https://towardsdatascience.com/agentic-rag-vs-classic-rag-from-a-pipeline-to-a-control-loop/)（2025）

| | 说明 |
|---|---|
| **现状** | `multistep.py` 用 `_TIME_KEYWORDS`、`_KNOWLEDGE_HINTS`、`_CALC_HINTS` 关键词列表决定工具选择，语义模糊时失效，新增工具需改硬编码逻辑 |
| **方案** | DeepSeek 已原生支持 function calling。把工具 schema 传给模型，让 LLM 自主决定调用哪个工具、传什么参数，实现"规则驱动 → LLM 规划驱动"的范式升级。这是 Agentic RAG 2025 的核心方向 |
| **改动范围** | 重写 `multistep.py` 的 `select_next_tool_call`，工具注册表 `tools/registry.py` 不变，接口层不变 |
| **优先级** | P1 — 解决 Agent 最核心的脆弱性 |

---

### T4 — Corrective RAG 自我反思与二次检索

状态：已完成（2026-04-11，已接入本地二次检索控制环与 partial 分支网络补搜，默认关闭）

**参考文献：** [Self-RAG / Corrective RAG](https://pinecone.io/learn/advanced-rag-techniques/)（2024）；[The RAG You Built Last Year Is Already Outdated](https://www.chanl.ai/blog/rag-evolved-five-architectures-replaced-basic-pipeline)（2025）

| | 说明 |
|---|---|
| **现状** | 检索后直接生成答案，无质量校验。知识库覆盖不全时会产生幻觉或"我不知道"式回答 |
| **方案** | 在生成前增加"相关性评分"步骤：让 LLM 判断检索结果是否足够回答问题（相关 / 部分相关 / 不相关）。不相关时触发二次检索（换关键词或扩大 top-k），部分相关时补充网络搜索（可选）。形成"检索 → 评估 → 再检索 → 生成"的控制环 |
| **改动范围** | `chains/rag.py` 增加 `grade_documents()` 步骤；`local_kb.py` 暴露二次检索入口；受 `ENABLE_CORRECTIVE_RAG` 开关控制 |
| **优先级** | P2 — 需要额外 LLM 调用，有延迟成本，建议在 T1/T2/T3 落地后再做 |

---

### T5 — BM25 替换为 rank_bm25 + 索引持久化

状态：已完成（2026-04-11，已落地持久化与回退链路；安装 `rank_bm25` 后自动切主后端）

**参考文献：** [Improving Retrieval Accuracy in RAG Systems](https://www.rohan-paul.com/p/improving-retrieval-accuracy-in-rag)（2025）；[Advanced RAG Optimization: Chunking, Re-ranking & Hybrid Retrieval](https://chatsy.app/blog/advanced-rag-optimization)（2025）

| | 说明 |
|---|---|
| **现状** | BM25 完全手写实现（`collect_lexical_candidates`），每次检索全量重建 doc_infos，无缓存；RRF 权重（0.35/0.25 等）硬编码 |
| **方案** | 换用 `rank_bm25` 成熟库，在知识库构建时序列化 BM25 索引到磁盘（与 FAISS 索引同目录），检索时直接加载。RRF 权重改为 `kb_settings.yaml` 可配置参数 |
| **改动范围** | `local_kb.py` 的 `collect_lexical_candidates`；`kb_incremental_rebuild.py` 构建时同步序列化 BM25 索引 |
| **优先级** | P2 — 对大知识库性能提升明显 |

---

### T6 — chunk cache 改用 numpy 存储

状态：已完成（2026-04-11，兼容旧版 JSON 内联 embedding cache）

**参考文献：** [Knowledge Base Incremental Rebuild Optimization](https://ragflow.io/blog/rag-review-2025-from-rag-to-context)（2025）

| | 说明 |
|---|---|
| **现状** | `kb_incremental_rebuild.py` 将 embedding 向量序列化为 JSON 存储浮点数组，体积大、读写慢 |
| **方案** | 改用 `numpy.save/load`（`.npy` 格式），同等数据量体积缩小 3–5 倍，读写速度提升 10x+。chunk 文本部分继续用 JSON，向量部分单独存 `.npy` |
| **改动范围** | `kb_incremental_rebuild.py` 的 `save_chunk_cache` / `load_chunk_cache` 两个方法 |
| **优先级** | P1 — 改动极小，收益立竿见影 |

---

### T7 — Agent 长期记忆（xMemory 三层架构）

**参考文献：** [Agentic RAG vs Traditional RAG](https://mem0.ai/blog/agentic-rag-vs-traditional-rag-guide)（2025）；[Agentic RAG Survey](https://arxiv.org/html/2501.09136v2)（2025.01）

| | 说明 |
|---|---|
| **现状** | Agent 只有请求内的 `history` 短期上下文，跨请求无记忆，无法回答"上次我们讨论过什么"类问题 |
| **方案** | 详见第七章 xMemory 改造方案。核心：`raw turns → episodes → semantic facts` 三层存储，Agent 请求前自动检索 semantic + episode 摘要注入上下文，与知识库证据分开组织 |
| **改动范围** | 新增 `app/services/memory_service.py`；`/chat/agent` 新增可选 `session_id`；不改 `/chat/rag` 主链路 |
| **优先级** | P3 — 工作量较大，建议 T1–T5 完成后再启动 |

---

### 落地优先级汇总

| 优先级 | 技术 | 预估工作量 | 核心收益 |
|--------|------|-----------|---------|
| P1 | T6 chunk cache → numpy | 0.5天 | 索引构建速度 10x |
| P1 | T1 Multi-Query 扩展 | 1天 | 召回率提升 |
| P1 | T2 HyDE 假设文档嵌入 | 1天 | 专业领域 Top1 命中率 |
| P1 | T3 LLM Tool Calling | 1–2天 | Agent 稳定性根治 |
| P2 | T5 BM25 持久化 | 1天 | 大知识库检索性能 |
| P2 | T4 Corrective RAG | 2天 | 幻觉率下降 |
| P3 | T7 xMemory 长期记忆 | 1–2周 | 跨请求记忆能力 |

---

## 七、xMemory 映射到当前项目的改造方案

### 6.1 摘要

目标是把 xMemory 映射成**新增的 Agent 长期记忆分支**，而不是重写当前通用文档 RAG。第一版采用**独立 memory store**，范围只覆盖 `/chat/agent`，不开放独立 memory API，不改现有 `/chat/rag` 主链路。

第一版只做到 `raw turns -> episodes -> semantic facts` 三层，不做 `theme` 层。核心收益是：把当前只靠 `history` 的短期上下文，升级为可跨请求复用、可去重、可按语义聚合的长期记忆，同时保持现有知识库检索和多模态能力不受影响。

### 6.2 关键改造

#### 6.2.1 新增独立 Agent Memory 子系统

- 新建独立存储根目录，采用 `data/agent_memory/<session_id>/` 结构，不与现有知识库目录混用。
- 每个 `session_id` 下保存三类数据：
  - `turns.jsonl`：原始会话 turn，追加写入，作为最底层 faithful memory。
  - `episodes.jsonl`：由连续 turn 聚合出的 episode 记录，包含 `episode_id`、turn 范围、时间戳、标题、摘要、参与角色、状态。
  - `semantics.jsonl`：从 episode 提炼出的长期事实，包含 `semantic_id`、事实文本、来源 `episode_id`、事实类别、置信度、失效/更新时间。
- 为 `episodes` 和 `semantics` 分别建立独立向量索引；`turns` 不直接做主检索，只作为低层展开证据。

#### 6.2.2 第一版 memory construction 策略

- `turns -> episodes`：优先采用启发式分段，不在 v1 依赖复杂 LLM 边界检测。
- 分段信号固定为：时间间隔、显式换题、工具调用阶段结束、用户任务切换、知识库切换。
- 默认策略：达到一定 turn 数或触发明显边界时封存一个 episode。
- `episodes -> semantics`：用 LLM 对 episode 做“长期有价值事实”抽取。
- 只抽取持久、可检索、可独立理解的信息，如偏好、约束、目标、身份、项目状态、关键决策。
- 不抽取短暂寒暄、一次性情绪、无后续价值的回复。
- 提取失败时保留 episode，不阻断主流程。
- v1 不做 `theme` 层，也不做复杂 merge/split；但 episode 和 semantic 记录都预留 `parent/children/source_ids` 字段，方便二阶段升级。

#### 6.2.3 memory retrieval 按 xMemory 思路做 top-down

- Agent 请求进入后，先做 memory retrieval，再做工具规划和知识库检索。
- memory retrieval 采用两阶段：
  - Stage A：先检索 `semantic` 层，取相关且去重后的事实集合，作为高层代表节点。
  - Stage B：根据命中的 `semantic_id -> episode_id` 反查 episode，只补入能增加新信息的 episode 摘要；原始 turn 只在证据不足或存在冲突时再展开。
- 上下文扩展遵循“先抽象、后展开”的固定策略：
  - 默认只注入 `semantic + episode summary`
  - 若多条 semantic 冲突、semantic 过短、或问题包含明显时间/因果依赖，再补原始 turn 片段
- 明确不做通用 RAG 的固定 top-k 拼接；memory 侧目标是**更少 token、更多非重复证据**。

#### 6.2.4 Agent 接入方式

- `/chat/agent` 新增可选 `session_id`，作为长期记忆主键。
- v1 不新增公开的 memory tool；memory retrieval 作为 Agent 执行前置步骤自动运行。
- Agent 规划 prompt 增加 memory context 段，优先用长期记忆回答“你之前说过/上次我们讨论过/我偏好什么/当前项目进行到哪”的问题。
- 最终回答阶段把 memory context 与知识库 references 分开组织：
  - `长期记忆证据`
  - `知识库证据`
- 若 memory 与知识库冲突，优先提示冲突，不自动融合。

#### 6.2.5 配置与可观测性

- 新增 Agent memory 配置，默认关闭：
  - `ENABLE_AGENT_MEMORY=false`
  - `AGENT_MEMORY_MAX_RECENT_TURNS`
  - `AGENT_MEMORY_EPISODE_MAX_TURNS`
  - `AGENT_MEMORY_SEMANTIC_TOP_K`
  - `AGENT_MEMORY_EPISODE_TOP_K`
  - `AGENT_MEMORY_ENABLE_TURN_EXPANSION`
- 复用现有 trace 体系，新增：
  - `memory_build_trace.jsonl`
  - `memory_retrieval_trace.jsonl`
- trace 至少记录：
  - 当前 `session_id`
  - 新增 episode 数
  - 新增 semantic 数
  - 命中 semantic / episode / turn 数
  - 最终 memory token 预算占用
- v1 不做 UI 页面；只要求后端 trace 可用于调试。

### 6.3 对外接口与类型变化

- `AgentChatRequest`
  - 新增 `session_id: str | None`
  - 未传时默认无长期记忆，只走当前行为，保证兼容
- `AgentChatResponse` 与流式 `done` 事件
  - 回传 `session_id`
  - 可选回传 `memory_overview`，至少包含 `semantic_hits`、`episode_hits`、`turn_hits`、`used_memory`
- 内部新增 memory record 类型
  - `MemoryTurn`
  - `MemoryEpisode`
  - `MemorySemantic`
  - `MemoryOverview`

### 6.4 分阶段实施

#### Phase 1：最小可落地版

- 加 `session_id`
- 建独立 memory store
- 记录 raw turns
- 按启发式生成 episode
- 从 episode 提取 semantic
- Agent 自动检索 semantic + episode 摘要
- 只做 trace，不做 UI

#### Phase 2：xMemory 风格检索增强

- semantic 检索增加去重与覆盖优先，而不是纯相似度 top-k
- episode inclusion 改成“增益式补充”，避免重复 episode 一起进入上下文
- 冲突语义触发原始 turn 展开
- 在最终回答中区分 memory 证据与 KB 证据

#### Phase 3：高级增强

- 引入 `theme` 层
- 支持 semantic reassignment / merge / split
- 增加 memory-only 评测集
- 再考虑调试 API 或 UI

### 6.5 测试计划

- 兼容性
  - 不传 `session_id` 时，`/chat/agent` 行为与当前一致
  - `/chat/rag` 完全不受影响
- 记忆写入
  - 同一 `session_id` 连续多次请求后，turn/episode/semantic 正常累积
  - 更换 `session_id` 不串记忆
- 检索正确性
  - 用户在前几轮提到偏好、约束、项目状态，后几轮能被 semantic 命中
  - 涉及时间链的问题，能从 semantic 回溯到 episode，必要时再展开 turn
  - 重复表达不会导致 memory context 大量冗余
- 冲突处理
  - 同一事实被更新时，新旧 semantic 不应同时被无差别引用
  - memory 与 KB 冲突时，答案要明确指出冲突来源
- 质量与效率
  - 相比直接拼接全部 history，memory context token 数明显下降
  - 在跨请求问题上，答案质量优于仅靠 `history` 的版本

### 6.6 假设与默认值

- 这版方案只针对 Agent 长期记忆，不推广到通用文档知识库主链路。
- memory store 独立存储，但向量索引和 embedding 设施可复用现有实现。
- v1 采用启发式 episode 分段，避免一开始把边界检测完全交给 LLM。
- v1 的 semantic 抽取允许失败回退，不要求严格 schema 才继续主流程。
- v1 不做 theme、retroactive merge/split、独立 memory API、UI 调试页。

## 7. RAG 事实正确率优先优化方案

### 7.1 摘要

- 第一优先直接提升 `factual_correctness`：保留当前 `deepseek-chat`，先不换模型，先优化 prompt、证据组织和复审链路。
- 第二优先提升检索出来片段的“贴题度”：这轮先不调 `CHUNK_SIZE/CHUNK_OVERLAP`，不触发全量重建，只做无需重建的检索与生成层优化。
- 第三优先压制“答非所问但看起来像在答”：将非流式 RAG 改成“先抽证据，再组织答案，再做一次完整性复审”的三段式生成。
- 生效范围是全局非流式路径：`/chat/rag`、UI 非流式调用、`eval_crud_rag.py`、`eval_ragas.py` 共用同一套生成逻辑。
- 本轮不改流式回答协议和行为；外部 `references` 响应保持不变，只压缩 prompt 内部上下文。

### 7.2 关键实现

- 在 `app/chains/rag.py` 内把非流式答案生成改成三段式：
  - `evidence extraction`
    - 输入压缩后的上下文和 `coverage_requirements`
    - 只输出结构化 JSON，字段固定为 `facts`、`unknown_points`、`checklist_coverage`
    - 每条 fact 必须直接可回溯到给定证据，不允许补充常识
  - `answer drafting`
    - 最终答案只能基于上一步抽取出的 facts 生成
    - prompt 明确要求“必须严格依据 context 作答”“证据不足时直接说无法确定”“默认先直接回答，不写证据来源套话，不复述题干”
  - `completeness review`
    - 保留一次复审，但输入改成“facts + checklist + 首稿答案”
    - 只负责补漏和去掉答非所问内容，不允许新增未在 facts 中出现的事实
- 升级普通文本 RAG prompt：
  - 明确加入硬约束：
    - 禁止使用上下文外事实
    - 不确定就明确说无法确定
    - 不得把相似事实当成目标答案
  - 若问题包含多个并列要求，答案必须按 checklist 顺序逐项输出
  - 若是单事实问题，第一句必须直接给结论
  - 只有当用户显式询问“依据/来源/为什么/从哪看出”时，才允许在答案里附一行简短依据说明
  - 默认不在正文里写“根据上下文/根据文本证据/来源如下”等模板化前缀
- 在 `app/chains/rag.py` 新增内部证据压缩视图，替代当前直接拼整段 `content`：
  - 先按 `sample_id/source_path` 聚合同组证据
  - 同组内按归一化文本指纹去重，优先保留 `evidence_summary` 明确、`relevance_score` 高、内容更短的块
  - 压缩后最多保留 `3` 个主证据块加 `1` 个补充证据块
  - 总字符预算控制在 `1800-2200`
  - 每个证据块只保留：来源标识、简短摘要、必要的原文片段
  - 不再把重复 sibling 文档整段灌给模型
- 在 `app/retrievers/local_kb.py` 做不需要重建的检索侧保护：
  - `query rewrite` 永远保留原始 query 为第一检索项
  - 不允许改写结果替代原问
  - 对改写 query 增加长度、实体保留、数值保留约束
  - 明显泛化或丢实体时直接回退原问
  - 维持现有 reranker
  - 最终供答案生成使用的内部证据集合收敛到更少、更贴题的代表片段，不把同样本的重复材料全部喂给模型
- 保持外部接口稳定：
  - 不修改 `ChatResponse` / `RetrievedReference` / `AgentChatResponse` 的对外字段
  - 可在 answer trace 中新增内部观测项：
    - `compressed_reference_count`
    - `evidence_fact_count`
    - `unknown_point_count`
    - `completeness_review_revised`
  - 这些字段仅用于调试和评测分析

### 7.3 公共接口与行为

- 外部 API 响应结构不变，`references` 继续返回原始检索结果。
- 只改非流式 `generate_rag_answer()`。
- `stream_rag_answer()` 本轮不接入三段式证据抽取与复审。
- 不改 chunk 参数、向量库结构、知识库重建流程，这轮不要求重新建库。

### 7.4 测试与验收

- 单元测试
  - `coverage_requirements` 能正确拆分并列问题、清单问题、单事实问题
  - 证据压缩能把同 `sample_id` 下重复 sibling 文档合并，且字符预算受控
  - 证据抽取 JSON 解析失败、复审失败或异常时，必须回退到上一阶段结果，不中断回答
  - 单事实问题生成结果不得默认附带“根据上下文/来源/证据编号”等套话
- 回归与评测
  - 先跑官方 split `quest_answer` 的 CRUD 30 条与 RAGAS 30 条
  - 以当前基线作为验收下限：
    - `llm_context_recall >= 0.95`
    - `faithfulness >= 0.72`
    - `factual_correctness > 0.467`
    - `answer_char_f1 >= 0.34`
  - 至少抽查 5 条之前“答得像对但不够贴 gold”的 case，确认答案更短、更直接、更少证据套话
- 重点验收场景
  - 多并列字段问题：金额 + 数量 + 名称
  - 清单问题：原因 / 措施 / 职责 / 步骤
  - 证据不足问题：能明确输出“根据当前检索到的内容，无法确定”而不是硬答
  - 重复证据问题：同 `sample_id` 多份 sibling 文档存在时，最终答案不应明显变得更啰嗦

### 7.5 假设与默认值

- 当前 `deepseek-chat` 继续作为答案生成模型，这轮先验证“结构优化”收益，不把模型切换和生成链路改造混在一起。
- 这轮不调 `CHUNK_SIZE/CHUNK_OVERLAP`，不做全量重建。
- 这轮将“提高事实正确率”优先于“展示证据解释性”；证据展示保留在 `references`，不强行塞进答案正文。

---

## 八、结合当前评测结果的下一步 RAG 优化建议

### 8.1 摘要

- 这一部分不是重新设计整套 RAG，而是基于 `2026-04-17` 这轮 `CRUD + Domain + RAGAS` 评测结果，给出下一步最值得优先推进的优化方向。
- 当前系统已经具备 `查询改写`、`多查询检索`、`混合检索`、`rerank`、`small-to-big` 和 `多模态证据组织` 等能力，因此下一步重点不是“从 0 增加新模块”，而是围绕真实薄弱点做针对性优化。
- 从结果上看，当前最突出的问题不是 `CRUD`，而是 `Domain`，尤其是 `time-sensitive_qa`、`multi-doc_qa` 以及生成侧的 `factual_correctness`。

### 8.2 当前评测暴露出的主要问题

| 问题方向 | 当前表现 | 更准确的理解 |
|--------|---------|-------------|
| 时间敏感问答 | `time-sensitive_qa` 检索指标最低 | 不是系统完全不会答，而是“时间条件没有被稳定保留下来”，导致证据召回和排序都偏弱 |
| 多文档问答 | `multi-doc_qa` 的 `Recall@5` 高，但 `NDCG@5` 一般 | 说明相关材料通常能找到，但前排结果的证据覆盖和排序还不够好 |
| 生成事实正确性 | `Domain` 的 `Factual Correctness` 很低 | 说明模型有时没有严格贴着检索证据回答，或对多条证据整合不完整 |
| 当前总体现象 | 检索“能找到”多于“排得准”，生成“能回答”多于“答得严” | 下一步优化应当从“更准的证据”与“更严的生成”两侧同时推进 |

### 8.3 第一优先级：优化时间敏感检索

#### 8.3.1 原因

- 这是当前 `Domain` 中最弱的一类任务。
- 这类问题往往包含 `当前`、`最新`、`截至某年`、`现任` 之类时间约束，一旦查询改写或重排阶段把时间条件弱化，最终结果就会偏掉。

#### 8.3.2 建议做法

- 为 chunk 补充时间元数据：
  - 例如年份、发布日期、更新时间、事件发生时间。
- 在查询改写阶段显式保留时间约束：
  - 不允许把 `现在`、`当前`、`截至 2024 年` 这类条件改写丢失。
- 在重排阶段加入时间加权：
  - 对时间更接近、标题中直接出现目标实体和时间信息的证据提高排序分数。
- 对时间敏感问题单独做意图识别：
  - 一旦识别为时间敏感问题，就启用更保守的 query rewrite 策略，并适当提高精排阈值。

#### 8.3.3 预期收益

- 优先提升 `time-sensitive_qa` 的 `Recall@5` 和 `MRR`。
- 这一项如果做好，后续生成侧的“答错当前信息”问题也会同步减少。

### 8.4 第二优先级：优化多文档证据覆盖

#### 8.4.1 原因

- 当前 `multi-doc_qa` 更像是“能召回到部分相关材料”，但前几条结果不一定能完整覆盖回答所需的多个信息点。
- 这会直接影响后续生成，容易出现“答到一半”“只覆盖其中一个来源”的情况。

#### 8.4.2 建议做法

- 在重排后增加“多源覆盖”约束：
  - 不要让前几条结果全部来自同一份材料或同一类 chunk。
- 对并列型问题增加子问题拆分：
  - 例如“分别是什么”“共同点和不同点是什么”这类问题，先拆成多个检索子目标，再汇总答案。
- 在生成前做证据覆盖检查：
  - 如果问题本身要求多个对象、多项条件或多步过程，就先检查当前证据是否覆盖到每一项。

#### 8.4.3 预期收益

- 提升 `multi-doc_qa` 的 `NDCG@5` 和生成侧完整性。
- 减少“检索有了，但答案仍然不完整”的情况。

### 8.5 第三优先级：提升生成侧事实正确性

#### 8.5.1 原因

- 当前生成阶段的主要问题，不是完全脱离检索内容乱答，而是“部分依据检索、部分自由发挥”，或者证据整合不够完整。
- 从 `RAGAS` 结果看，`faithfulness` 和 `factual_correctness` 仍然有明显优化空间。

#### 8.5.2 建议做法

- 继续强化“只基于证据作答”的 Prompt 约束：
  - 证据不足时直接回答“根据当前检索结果无法确定”。
- 对列表题、比较题、并列题采用结构化输出：
  - 按条作答，减少漏项。
- 在生成后增加一次证据覆盖复查：
  - 不是重新生成更多内容，而是检查是否漏掉必要条件、时间点、人物、数量等关键槽位。
- 优先优化非流式回答链路：
  - 因为评测脚本主要走这一条路径，更方便和 `RAGAS` 指标闭环验证。

#### 8.5.3 预期收益

- 重点提升 `Factual Correctness` 和 `Answer Relevance`。
- 让答案更短、更直接，同时更少出现“看起来合理但不完全贴 gold”的情况。

### 8.6 第四优先级：优化 chunk 与元数据设计

#### 8.6.1 原因

- 当前检索问题并不完全来自模型能力，很多时候是因为 chunk 被切碎后，时间、标题、章节信息没有被充分保留下来。
- 特别是领域文档、公告通知、制度类文本，对标题和时间字段很敏感。

#### 8.6.2 建议做法

- 从固定长度切分逐步过渡到更偏语义的切分。
- 保留标题、章节名、时间、来源路径到 metadata。
- 对“标题 + 首段”建立更高权重：
  - 这类内容通常最能概括文档主题。
- 这部分属于需要重建索引的优化，建议在前面几项完成后再统一推进。

### 8.7 第五优先级：继续扩充评测闭环

#### 8.7.1 原因

- 当前已经有 `Recall@k`、`MRR`、`NDCG` 以及 `RAGAS` 的基础评测链路，但生成侧样本量仍然偏小。
- 如果后续只看总均值，很容易掩盖 `time-sensitive_qa` 这类薄弱任务。

#### 8.7.2 建议做法

- 检索侧继续固定保留：
  - `Recall@5`
  - `MRR`
  - `NDCG@5`
- 生成侧继续固定保留：
  - `Context Recall`
  - `Faithfulness`
  - `Factual Correctness`
  - `Answer Relevance`
- 每次优化后都按任务类型分别看结果：
  - `extractive_qa`
  - `conversation_qa`
  - `multi-doc_qa`
  - `time-sensitive_qa`
  - `structured_qa`
- 先维持“小批量可重复评测”，再逐步扩大到更大样本。

### 8.8 推荐推进顺序

| 阶段 | 优先内容 | 主要目标 |
|------|---------|---------|
| 第一阶段 | 时间敏感检索优化 | 先解决当前最弱任务，提升 `time-sensitive_qa` |
| 第二阶段 | 多文档证据覆盖优化 | 提升 `multi-doc_qa` 的排序和答案完整性 |
| 第三阶段 | 生成侧事实正确性优化 | 提升 `Factual Correctness` 与 `Answer Relevance` |
| 第四阶段 | chunk 与 metadata 优化 | 为后续更大规模评测打基础 |
| 第五阶段 | 扩大评测样本与回归流程 | 形成稳定的优化闭环 |

### 8.9 当前阶段的总体判断

- 当前系统已经不是“基础 RAG 能不能跑起来”的阶段，而是进入了“针对不同任务类型做精细化优化”的阶段。
- 因此下一步最值得优先做的，不是继续泛泛地增加新模块，而是围绕 `time-sensitive_qa`、`multi-doc_qa` 和生成侧 `factual_correctness` 做定向改进。
- 如果只能先做一项，建议优先处理时间敏感检索；如果可以并行推进，则建议同时推进“时间敏感检索 + 生成事实正确性”两条线。
